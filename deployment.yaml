apiVersion: apps/v1
kind: Deployment
metadata:
  name: trend-app-deployment
  labels:
    app: trend-app
spec:
  replicas: 2
  selector:
    matchLabels:
      app: trend-app
  template:
    metadata:
      labels:
        app: trend-app
    spec:
      containers:
      - name: trend-app
        image: 743808052586.dkr.ecr.ap-south-1.amazonaws.com/trend-app:latest
        ports:
        - containerPort: 80   # FIXED ✅

